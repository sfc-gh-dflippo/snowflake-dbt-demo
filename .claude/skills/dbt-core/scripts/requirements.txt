# name: Python Requirements
# description: Supporting Python tools (Snowflake CLI, Snowpark, etc.) for dbt development
# dbt itself is installed via the Fusion installer (see install-dbt.sh), not pip.

### How to use this file:
# python -m venv .venv
# source .venv/bin/activate  # On Windows: .venv\Scripts\activate
# pip install -U -r requirements.txt

keyring
pandas
pip
pip-system-certs
#snowflake-ml-python
snowflake-snowpark-python
streamlit
uv

# snowflake-cli and schemachange are installed as standalone uv tools, not pip deps:
#   export UV_SYSTEM_CERTS=true   # make uv trust the system cert store (corporate firewall/SSL)
#   uv tool install snowflake-cli
#   uv tool install schemachange
