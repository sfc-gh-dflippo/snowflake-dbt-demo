[project]
name = "testbed-generator"
version = "0.1.0"
description = "Orchestration driver for the Synthetic Testbed Generator mine phase"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = ["pytest>=9.0.3"]

[tool.uv]
package = false
