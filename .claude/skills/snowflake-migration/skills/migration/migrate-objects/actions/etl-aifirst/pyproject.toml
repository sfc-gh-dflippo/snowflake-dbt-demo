[project]
name = "etl-aifirst"
version = "0.1.0"
description = "AI-First ETL convert scripts for SnowConvert AI migrations"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["scripts"]
