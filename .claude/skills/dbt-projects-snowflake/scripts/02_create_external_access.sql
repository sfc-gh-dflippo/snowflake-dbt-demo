[{"text": "-- Create External Access Integration for dbt deps\\n-- Allows dbt to download packages from hub.getdbt.com and codeload.github.com\\n-- Usage: snow sql -f 02_create_external_access.sql -c default\\n-- Requires: ACCOUNTADMIN role\\n\\n-- Create NETWORK RULE\\nCREATE OR REPLACE NETWORK RULE dbt_network_rule\\n  MODE = EGRESS\\n  TYPE = HOST_PORT\\n  VALUE_LIST = (\\n    'hub.getdbt.com',\\n    'codeload.github.com'\\n  );\\n\\n-- Create EXTERNAL ACCESS INTEGRATION\\nCREATE OR REPLACE EXTERNAL ACCESS INTEGRATION dbt_ext_access\\n  ALLOWED_NETWORK_RULES = (dbt_network_rule)\\n  ENABLED = TRUE;\\n\", \"type\": \"text\"}]