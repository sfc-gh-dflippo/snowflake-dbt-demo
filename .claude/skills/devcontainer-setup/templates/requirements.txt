# Snowflake Python tools
# Note: dbt 2.0 (Fusion) is installed via the Fusion installer (curl install.sh),
#       and snowflake-cli / schemachange are installed globally via `uv tool install`.
snowflake-snowpark-python

# Development tools
streamlit
pandas

# Authentication helpers
keyring
pip-system-certs

# Package management
uv
