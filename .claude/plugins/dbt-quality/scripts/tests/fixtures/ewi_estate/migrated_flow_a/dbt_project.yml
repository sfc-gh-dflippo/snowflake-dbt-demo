name: "migrated_flow_a"
version: "1.0.0"
config-version: 2
profile: "YOUR_PROFILE_NAME"

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  migrated_flow_a:
    +tags: ["stabilization_test"]
