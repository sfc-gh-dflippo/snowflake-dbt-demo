-- MIG008: placeholder model (WHERE FALSE)
-- status: placeholder
-- awaiting logic conversion

select
    cast(null as integer) as invoice_id,
    cast(null as varchar) as customer_id,
    cast(null as decimal(18,2)) as amount
where false
